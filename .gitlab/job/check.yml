---

.check-lint:
  image: ubuntu:latest
  services:
    - node:latest
  script:
    # Install dependencies
    - yarn install
    # Run linter
    - yarn run lint
    # Run typescript linter
    - yarn run lint:types
